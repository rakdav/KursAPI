<UserControl x:Class="KursClient.Views.ChitateliView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KursClient.Views"
             xmlns:vm="clr-namespace:KursClient.ViewModels"
             xmlns:Menu="clr-namespace:KursClient.Utills"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Style="{StaticResource Page_Style}">
    <UserControl.DataContext>
        <vm:ChitateliViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="0,20,20,0" Margin="0,5,5,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                            Command="{Binding AddCommand}" Width="30"
                            Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/add.png"/>
                    </Button>
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                        Width="30"
                        Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/Del.png"/>
                    </Button>
                </StackPanel>
                <DataGrid ItemsSource="{Binding ChitateliList}"
                          AutoGenerateColumns="False"
                          Grid.Row="1" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Фамилия" Width="Auto" Binding="{Binding LastName}"/>
                        <DataGridTextColumn Header="Имя" Width="Auto" Binding="{Binding FirstName}"/>
                        <DataGridTextColumn Header="Адрес" Width="Auto" Binding="{Binding Address}"/>
                        <DataGridTextColumn Header="Телефон" Width="Auto" Binding="{Binding Phone}"/>
                        <DataGridTextColumn Header="Email" Width="Auto" Binding="{Binding Email}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
